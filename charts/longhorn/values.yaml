# https://github.com/longhorn/charts/tree/master/charts/longhorn
longhorn:
  persistence:
    defaultClass: false
    defaultClassReplicaCount: 2
  defaultSettings:
    # backupTarget: nfs://nfs-wd1to-0.nfs-wd1to.nfs-wd1to.svc.cluster.local:/storage
    defaultReplicaCount: 2
