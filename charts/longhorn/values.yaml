# https://github.com/longhorn/charts/tree/master/charts/longhorn
longhorn:
  persistence:
    defaultClass: false
    defaultClassReplicaCount: 2
  defaultSettings:
    backupTarget: nfs://nfs-backups-0.nfs-backups.nfs-backups.svc.cluster.local:/
    defaultReplicaCount: 2
