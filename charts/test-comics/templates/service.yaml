apiVersion: v1
kind: Service
metadata:
  name: {{ include "comics.fullname" . }}
  labels:
    {{- include "comics.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "comics.selectorLabels" . | nindent 4 }}
