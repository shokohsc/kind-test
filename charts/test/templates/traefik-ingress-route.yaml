---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  annotations:
    kubernetes.io/ingress.class: traefik
  name: test
  namespace: test
spec:
  entryPoints:
    - web
  routes:
  - kind: Rule
    match: Host(`test.kind.shokohsc.home`)
    priority: 0
    services:
    - kind: Service
      name: test-homer
      port: 8080
