apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "commander.fullname" . }}-test-connection"
  labels:
    {{- include "commander.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "commander.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
